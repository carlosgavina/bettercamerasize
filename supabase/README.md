# Supabase

This directory contains the database definition for Better Camera Size.

Recommended next steps:

1. install the Supabase CLI
2. run `supabase init` if we want the full local stack files
3. keep reviewed schema changes as SQL migrations in `supabase/migrations`

The initial migration in this repository focuses on the domain model first.
