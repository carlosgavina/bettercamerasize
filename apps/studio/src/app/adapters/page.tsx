import { StudioResourcePage } from "@/components/studio-resource-page";

export default function AdaptersPage() {
  return (
    <StudioResourcePage
      eyebrow="Catalog"
      title="Adapters"
      description="Manage real adapter products that back generic mount conversions, including added length and weight."
      view="adapters"
    />
  );
}
