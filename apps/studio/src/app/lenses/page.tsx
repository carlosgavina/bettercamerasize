import { StudioResourcePage } from "@/components/studio-resource-page";

export default function LensesPage() {
  return (
    <StudioResourcePage
      eyebrow="Catalog"
      title="Lenses"
      description="Manage native lens records, optical specs, physical dimensions, mount assignment, and review-ready media."
      view="lenses"
    />
  );
}
