import { StudioResourcePage } from "@/components/studio-resource-page";

export default function MountsPage() {
  return (
    <StudioResourcePage
      eyebrow="Compatibility"
      title="Mounts"
      description="Inspect mount records that drive native compatibility and adapter conversion paths."
      view="mounts"
    />
  );
}
