import { StudioResourcePage } from "@/components/studio-resource-page";

export default function CamerasPage() {
  return (
    <StudioResourcePage
      eyebrow="Catalog"
      title="Cameras"
      description="Create, search, and edit camera bodies with dimensions, weight, sensor format, and native mount assignment."
      view="cameras"
    />
  );
}
