import { StudioResourcePage } from "@/components/studio-resource-page";

export default function ConversionsPage() {
  return (
    <StudioResourcePage
      eyebrow="Compatibility"
      title="Conversions"
      description="Edit generic adapter paths, link real adapter products, and choose defaults for user-facing compatibility."
      view="conversions"
    />
  );
}
