import { StudioResourcePage } from "@/components/studio-resource-page";

export default function PreviewPage() {
  return (
    <StudioResourcePage
      eyebrow="Validation"
      title="Preview"
      description="Validate body-first adapter and lens filtering against the live local catalog."
      view="preview"
    />
  );
}
