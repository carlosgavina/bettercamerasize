import { StudioResourcePage } from "@/components/studio-resource-page";

export default function AssetsPage() {
  return (
    <StudioResourcePage
      eyebrow="Media"
      title="Assets"
      description="Upload, review, and approve product media with view, hood state, source, and calibration metadata."
      view="assets"
    />
  );
}
